databaseChangeLog:
  - changeSet:
      id: create-roles-table
      author: ZdravkoMladenov
      changes:
        - createTable:
            tableName: roles
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
              - column:
                  name: role
                  type: varchar(30)
                  constraints:
                    unique: true